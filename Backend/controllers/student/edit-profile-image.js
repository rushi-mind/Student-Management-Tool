const db = require('../../models');
const bcrypt = require('bcrypt');
const Joi = require('joi');

module.exports = (async (req, res) => {
    res.send('this feature is currently out of service. sorry for the inconvenience.');
});